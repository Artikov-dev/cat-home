# Database URL from Render PostgreSQL
# Get this from your Render dashboard > PostgreSQL > Connection > External Database URL
DATABASE_URL=postgresql://username:password@host:port/database

# Port (Render will set this automatically in production)
PORT=5000